# Memory Log - 2026-04-01

## Session Summary
- Operator: Sol
- Context: Building autonomous dev workflow on Termux/Android
- Goal: Establish skill base, integrate external tools, enable email-based verification

## Accomplishments

### Skills Installed (12 total)
1. **skill-health** - ecosystem auditor (99 health score)
2. **resource-check** - Termux resource monitoring (RAM, storage, battery, CPU, network)
3. **git-status-summary** - git repo overview with filtering
4. **lean-ctx** - token optimizer; binary installed, tested (37% savings on git status)
5. **openbrowser** - autonomous web browsing (needs Bun+Playwright setup)
6. **claude-mem** - persistent memory worker (OOM on Termux; needs external server)
7. **opensandbox** - sandboxed code execution (needs Docker)
8. **gstack** - reference for Claude Code modes (plan, review, ship, browse, qa, retro)
9. **chatterbox-tts** - TTS with voice cloning (needs PyTorch on x86_64)
10. **agentmail** - email automation (inbox, search, get, send, extract-codes, wait-for-code)
11. **ollama** - local LLM serving wrapper (scripts created; binary not yet installed)
12. **llmfit** - hardware-aware model selector (scripts created; Rust binary needs build)

### Newly Created Scripts
- agentmail: send, extract-codes, wait-for-code (enables autonomous email verification flows)
- ollama: list-models, pull, generate, embed, serve, rm
- llmfit: llmfit (main), scan, recommend, hardware

### External Repos Cloned (22 total)
Tier 1: ollama, gh-aw, langflow, n8n, system-prompts-and-models-of-ai-tools, openfang, claudecodeui, tgo, llmfit, picoclaw
Tier 2: droidclaw, off-grid-mobile, Qwen3.5, Verus-Mobile, Crucix, project-nomad, wifi-densepose, awesome-claude-skills, TradingAgents
Plus: chatterbox, claude-mem, gstack, OpenSandbox, openbrowser, lean-ctx (already used)

### Analysis & Strategy
- Repo scan revealed clear pattern: mobile-first, offline-capable AI agent stack
- Core pillars: orchestration (openfang, eliza, tgo), LLM serving (ollama), resource optimization (lean-ctx, llmfit), mobile integration (droidclaw, off-grid-mobile), autonomous actions (openbrowser, agentmail, chatterbox), knowledge (claude-mem)
- Gap: Need an agent OS to tie together; openfang or picoclaw are candidates
- Resource constraints on Termux: cannot run PyTorch (chatterbox), heavy Node services (claude-mem), Docker (opensandbox). These must run on external hardware.

### Configuration
- Agentmail API token stored in `/root/.openclaw/credentials/agentmail.json`
- Setup queue and skill registry created to track progress

## Technical Notes

### lean-ctx success
- Built from source: cargo install lean-ctx
- Installed to `/usr/local/bin/lean-ctx`
- Tested: `lean-ctx -c git status` showed 37% token savings
- Integration: use `tool("lean-ctx", "run", command="...")` for compressed outputs

### claude-mem limitation
- Bun install succeeded (1858 packages)
- Worker process killed by OOM during start
- Conclusion: Termux memory insufficient (~14GB total, worker needs >2GB). Must deploy on workstation/VPS.
- Workaround: use lean-ctx for token savings instead of persistent memory for now.

### agentmail extension
- Added send, extract-codes, wait-for-code
- Supports autonomous sign-up workflows: openbrowser fills form → wait-for-code → enter code
- Code extraction uses regex heuristics; works for 4-8 digit codes and verification links

## Decisions & Priorities

### Immediate Next Steps (from auto-dream)
1. Finish wrapping lightweight single-binary tools: droidclaw, off-grid-mobile, picoclaw
2. Build "repo scanner" to score and prioritize remaining repos
3. Create "skill import" command for ad-hoc repo wrapping
4. Consider skill marketplace concept for sharing

### Ecosystem Direction
- Focus on tools that work within Termux constraints (Rust/Go binaries, lightweight)
- Delegate heavy services (claude-mem, opensandbox, chatterbox) to external server when available
- Use agentmail for email verification autonomously during idle periods
- Integrate ollama + llmfit for local LLM serving once binary installed on suitable hardware

## Lessons Learned

- **Resource awareness is critical:** Not all tools can run on mobile; need to detect and offload
- **Skill wrapper pattern works well:** Simple Python scripts around CLI binaries enable uniform OpenClaw integration
- **Automation potential high:** agentmail's wait-for-code + openbrowser can fully automate many sign-up flows
- **Token economy matters:** lean-ctx provides immediate, measurable savings; should be default for all verbose commands
- **Registry needed:** As skills grow, tracking installed, pending, and external repos is essential; created skill_registry.json and setup_queue.json

## Questions for Sol
- Should we proceed with installing ollama binary on this Termux? (Likely fails due to architecture/performance)
- Do you have a server where we can deploy claude-mem, opensandbox, and potentially ollama for heavy lifting?
- Preferred order for wrapping remaining repos: droidclaw/off-grid-mobile/picoclaw vs gh-aw vs openfang?

## Metadata
- Total skills in system: 68 (after installing new ones)
- Health: 67 healthy, 1 error (canvas missing frontmatter)
- External repos cloned: 22
- Skills created this session: 7 (agentmail extended, ollama, llmfit, plus earlier batch)
